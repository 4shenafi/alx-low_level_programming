#include "main.h"
/**
  * _abs - changes the value into positive
  * @r: is a number passed to be checked
  * Return: always return value in abs value
  */
int _abs(int r)
{
	if (r >= 0)
		return (r);
	else
		return (-r);
}
