#include "main.h"
#include <string.h>
/**
  * _strlen - gives the length of string
  * @s: passed parameter
  * Return: returns the length of dtring
  */
int _strlen(char *s)
{
	return (strlen(s));
}
