#include "main.h"
#include <stdio.h>
#include <string.h>
/**
  * _strlen_recursion - returns recursion
  * @s: passed parameter
  * Return: returns
  */
int _strlen_recursion(char *s)
{
	return (strlen(s));
}
