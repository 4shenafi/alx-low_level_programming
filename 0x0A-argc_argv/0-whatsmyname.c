#include <stdio.h>
#include "main.h"
/**
 * main - a function prints name of file
 * Return: always return
 */
int mian(int argc, char* argv[])
{
    (void) argc;
    printf("%s", argv[0]);
    return (0);
}
